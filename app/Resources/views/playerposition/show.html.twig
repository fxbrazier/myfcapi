{% extends 'base.html.twig' %}

{% block body %}
    <h1>Playerposition</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ playerPosition.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ playerPosition.label }}</td>
            </tr>
            <tr>
                <th>Shortlabel</th>
                <td>{{ playerPosition.shortLabel }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('playerposition_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('playerposition_edit', { 'id': playerPosition.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
