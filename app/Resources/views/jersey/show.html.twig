{% extends 'base.html.twig' %}

{% block body %}
    <h1>Jersey</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jersey.id }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ jersey.img }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('jersey_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('jersey_edit', { 'id': jersey.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
